<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/styles/main.css' | url }}" />
</head>
<body>
    <header>
        {% include "header.njk" %}
    </header>
    <main class="container mx-auto prose lg:prose-xl">
        <h1>{{ title }}</h1>
        Datum: {{ page.date }}

        <ul>
        {% for category in categories %}
            <li><a href="../category/{{ category | lower | url }}">{{ category }}</a></li>
        {% endfor %}
        {% for tag in tags %}
            {% if tag != 'posts' %}
            <li><a href="../tags/{{ tag | url }}">{{ tag }}</a></li>
            {% endif %}
        {% endfor %}
        </ul>

        {{ content  | safe }}

        {% include "paging-posts.njk" %}
    </main>
</body>
</html>