<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ '/styles/main.css' | url }}" />
</head>
<body>
    <header>
        {% include "header.njk" %}
    </header>
    <main class="container mx-auto prose lg:prose-xl">
        <h1>{{ title }}</h1>
        {{ content | safe }}
    </main>
</body>
</html>